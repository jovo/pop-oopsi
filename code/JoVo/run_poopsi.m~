% run_poopsi
cd ~/Research/oopsi/pop-oopsi/code/scripts/
clear all
load('../../data/JoVo/sim1_data.mat')    % load data
smc = pop_oopsi(Cell,V);            % do everything
omega = GetOmega(V,smc);                 % plot some stuff

%%


figure(1), clf,
subplot(121), 
imagesc(Cell{1}.P.omega)

subplot(122), 
imagesc(omega)
